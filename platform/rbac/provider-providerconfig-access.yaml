apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: crossplane:provider:providerconfig-reader
rules:
  - apiGroups:
      - aws.upbound.io
    resources:
      - providerconfigs
      - providerconfigusages  # ← AJOUTÉ
    verbs:
      - get
      - list
      - watch
      - create      # ← AJOUTÉ pour créer les usages
      - update      # ← AJOUTÉ
      - patch       # ← AJOUTÉ
